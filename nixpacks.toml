# Nixpacks configuration for Railway
# WeasyPrint requires these system libraries for PDF generation

[phases.setup]
aptPkgs = [
    "libpango-1.0-0",
    "libpangocairo-1.0-0",
    "libcairo2",
    "libgdk-pixbuf2.0-0",
    "libffi-dev",
    "libgobject-2.0-0",
    "libglib2.0-0",
    "shared-mime-info"
]

[start]
cmd = "uvicorn api.analyze:app --host 0.0.0.0 --port ${PORT:-8000}"
